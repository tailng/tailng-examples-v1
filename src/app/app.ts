import { Component } from '@angular/core';
import { TngTag } from '@tailng-ui/ui/primitives';

@Component({
  selector: 'app-root',
  imports: [TngTag],
  templateUrl: './app.html',
  styleUrl: './app.css'
})
export class App {
}
