import { Component } from '@angular/core';
import { TngTooltip } from '@tailng-ui/ui/overlay';

@Component({
  selector: 'app-root',
  imports: [TngTooltip],
  templateUrl: './app.html',
  styleUrl: './app.css'
})
export class App {
}
