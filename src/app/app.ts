import { Component } from '@angular/core';
import { TngIcon } from '@tailng-ui/icons/icon'

@Component({
  selector: 'app-root',
  imports: [ TngIcon],
  templateUrl: './app.html',
  styleUrl: './app.css'
})
export class App {
}
