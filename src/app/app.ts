import { Component } from '@angular/core';
import {TngButton} from '@tailng-ui/ui/primitives'
@Component({
  selector: 'app-root',
  imports: [TngButton],
  templateUrl: './app.html',
  styleUrl: './app.css'
})
export class App {
}
